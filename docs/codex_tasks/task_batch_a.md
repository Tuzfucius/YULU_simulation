# 批次A任务跟踪

## 任务目标

在保持原有功能不变的前提下，先修复一组确定性的正确性问题，不进行大规模架构重构。

## 任务范围

批次 A 包含以下修复项：

- 修复 ML 数据集导出字段 schema 不一致问题
- 修复排队状态未注入规则上下文，导致默认排队规则失效的问题
- 修复自定义非整数门架位置导致门架 ID 冲突的问题
- 修复前端轨迹采样时驾驶风格字段漂移的问题

## 执行记录

### 任务 1：文档目录整理

- 状态：已完成
- 结果：
- 已将审查报告迁移到专属目录 `docs/codex_tasks/`
- 已为专属目录补充说明文档
- 已将该目录加入 `.gitignore`

### 任务 2：ML 数据集字段修复

- 状态：已完成
- 目标：
- 统一 `build_dataset()` 与特征提取输出字段

### 任务 3：排队规则上下文修复

- 状态：已完成
- 目标：
- 将排队检测结果注入 `AlertContext.queue_lengths`

### 任务 4：门架 ID 冲突修复

- 状态：已完成
- 目标：
- 为自定义门架生成稳定唯一的运行期 ID

### 任务 5：前端驾驶风格字段漂移修复

- 状态：已完成
- 目标：
- 修复轨迹采样使用错误字段导致 `driver_style` 降级为 `normal`

## 验证项

- Python 相关改动需要至少完成针对性导入/静态检查或最小运行验证
- 前端改动至少需要完成针对性构建或类型检查验证
- Python 相关改动已完成 `python -m py_compile` 验证
- 前端改动已完成针对性 `npm run build` 误差排查，旧的 `driverStyle` 报错已消失，剩余失败来自仓库既有 TypeScript 问题

## 完成标记

以下条目全部改为“已完成”后，表示批次 A 完成：

- 文档目录整理：已完成
- ML 数据集字段修复：已完成
- 排队规则上下文修复：已完成
- 门架 ID 冲突修复：已完成
- 前端驾驶风格字段漂移修复：已完成
- 验证：已完成
